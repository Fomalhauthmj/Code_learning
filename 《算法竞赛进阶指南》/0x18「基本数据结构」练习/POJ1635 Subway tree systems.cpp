#include<iostream>
#include<string>
using namespace std;
string a,b;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin>>n;
    while(n--)
    {
        cin>>a>>b;
    }
    system("pause");
    return 0;
}